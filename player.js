export default class Player {
    position;
    size;
    speed;

    constructor(position, size, speed) {
        this.position = position;
        this.size = size;
        this.speed = speed;
    }
} 